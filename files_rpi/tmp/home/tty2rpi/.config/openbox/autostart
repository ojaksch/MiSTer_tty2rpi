source ~/tty2rpi.ini
xset -b

systemctl --user start tty2rpi-socket.service tty2rpi-inotify.service

# Show IP addresses
sleep 1
echo "CMDSHOWIPS" > "${SOCKET}"
sleep ${IPTIMEOUT}

# Show Welcome screen
KILLPID feh
feh -q -F ~/tty2rpi-pics/tty2rpi.png
